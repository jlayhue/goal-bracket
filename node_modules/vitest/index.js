import test from 'node:test';
import assert from 'node:assert/strict';
function describe(name, fn) { test(name, fn); }
const it = test;
function expect(value) {
  return {
    toBe: (expected) => assert.strictEqual(value, expected),
    toEqual: (expected) => assert.deepStrictEqual(value, expected),
    toBeTruthy: () => assert.ok(value),
    toBeFalsy: () => assert.ok(!value),
    toThrow: (err) => assert.throws(value, err)
  };
}
export { test, it, describe, expect, assert };
export default { test, it, describe, expect, assert };
