version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**

backend:
  phases:
    build:
      commands:
        - npx ampx generate outputs --app-id d20aubldexn0jj --branch dev
  artifacts:
    baseDirectory: amplify
    files:
      - '**/*'
